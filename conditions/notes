//CONDITIONS
//- conditions used to determine whether
//condition ? true_val : false_val
// if condition is true then true_val else false_val

// count = var.create ? 3 : 0
// if var.create is true then count is 3 else 0
variable "create"{
    default = TRUE
}
//count = var.ENV == "PROD" ? 1 : 0
//it is majorly used with count ,but can also be used with any argument also